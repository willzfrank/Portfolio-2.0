import Link from 'next/link';
import {
  ScrollingText,
  ChatContainer,
  ScrollingTextContainer,
} from './ChatStyles';

const ContinuousScrollingText = () => {
  return <ChatContainer></ChatContainer>;
};

export default ContinuousScrollingText;
