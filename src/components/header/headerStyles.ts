import styled from 'styled-components';

export const HeaderTitle = styled.h1`
  font-size: 8vw;
  line-height: 1em;
  word-break: break-word;
  hyphens: auto;
  overflow: hidden;
  margin-bottom: 0.4em;
`;
